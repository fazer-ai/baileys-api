import { describe, it } from "node:test";

describe("normalizeBrazilPhoneNumber", () => {
  describe("#normalizeBrazilPhoneNumber", () => {
    describe("when the phone number matches the regex pattern", () => {
      it.todo("returns the phone number with the extra digit 9", () => {
        // Test logic to be implemented
      });
    });
    describe("when the phone number does not match the regex pattern", () => {
      it.todo("returns the same phone number", () => {
        // Test logic to be implemented
      });
    });
  });
});
